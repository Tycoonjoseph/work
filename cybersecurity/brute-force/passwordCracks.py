from string import digits, ascii_letters, punctuation, whitespace, printable, hexdigits, octdigits as all_characters
for i in all_characters:
    for j in all_characters:
        for k in all_characters:
            for l in all_characters:
                for m in all_characters:
                    for n in all_characters:
                        for o in all_characters:
                            for p in all_characters:
                                print(i + j + k + l + m + n + o + p)